import { makeStyles } from "@material-ui/core/styles";
const useStyles = makeStyles(() => ({
  root: {
    width: "100%",
  },
  container: {
    maxHeight: 600,
  },
  demo: {
    marginBottom: "15px",
  },
}));

export default useStyles;
